#pragma once
// Stub — not used by the mock HalDisplay.h but guards against accidental includes.
class EInkDisplay {
 public:
  static constexpr int DISPLAY_WIDTH = 480;
  static constexpr int DISPLAY_HEIGHT = 800;
};
