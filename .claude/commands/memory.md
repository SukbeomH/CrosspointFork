Read `.hxsk/skills/memory-protocol/SKILL.md` and follow its workflow exactly.

Memory operation: file-based recall/store protocol. Use `bash .hxsk/hooks/md-recall-memory.sh <query>` for search, `bash .hxsk/hooks/md-store-memory.sh` for storage.

Arguments: $ARGUMENTS
